# Vercel — 프론트엔드(Next.js) 환경 변수 (Production / Preview / Development)
#
# Vercel 대시보드 → Project → Settings → Environment Variables 에 동일하게 등록.
# 키 앞에 NEXT_PUBLIC_ 가 붙은 값만 브라우저에 노출됨에 주의.

# 백엔드 베이스 URL — Cloudflare Tunnel 로 노출한 도메인.
NEXT_PUBLIC_API_URL=https://api.ktagent.me
